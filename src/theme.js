import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
    type: "light",
    primary: {
        main: "#673ab7",
    },
    secondary: {
        main: "#4db6ac",
    },
};

export default createMuiTheme({ palette });
